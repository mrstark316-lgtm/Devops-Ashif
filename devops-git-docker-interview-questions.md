# DevOps Interview Questions

---

## SDLC & Methodologies

- What is SDLC (Software Development Life Cycle)?
- What is the difference between Waterfall and Agile methodology?
- What is DevOps?
- What is the difference between a Frontend and a Backend application?

---

## Git Basics

- What is Git?
- What is the difference between a Centralized VCS and a Distributed VCS?
- What is the lifecycle of Git?
- What is a Git branch?
- What is a Git branching strategy?
- What is the difference between `git branch <NEW_BRANCH>` and `git checkout -b <NEW_BRANCH>`?
- What is the difference between `git revert`, `git restore`, `git rebase`, and `git cherry-pick`?

---

## GitHub & Collaboration

- What is GitHub?
- What is `git push`?
- What is `git pull`?
- What is the difference between `git fetch`, `git pull`, and `git clone`?
- What is the difference between GitHub and GitLab?
- What is a Fork in GitHub?
- What is a Pull Request (PR) in GitHub?
- What is a code review?
- How do you push changes to a DEV branch?

---

## Containers & Docker

- What is the difference between Monolithic and Microservices architecture?
- What is Containerization?
- What is the difference between a VM and a Container?
- What is a Dockerfile?
- What is `docker build`?
- What is `docker run`?
- What is the difference between `docker create`, `docker start`, and `docker run`?
- What is `docker ps`?
- What is `docker images`?
- What is the difference between an Image and a Container?
- What is port mapping in Docker?
- What is the difference between `-p` and `-P` in Docker?
- What are the instructions used in a Dockerfile?
- What is the difference between the `ADD` and `COPY` instructions in a Dockerfile?
- What is the difference between `RUN`, `CMD`, and `ENTRYPOINT` in a Dockerfile?
- What are the different Docker network modes?
- What is Docker Compose?

---

## Kubernetes

- What is Kubernetes?
- What is the architecture of Kubernetes?
- What is a Node in Kubernetes?
- What is a Pod in Kubernetes?
- What is a Deployment in Kubernetes?
- What is a Namespace in Kubernetes?
- What is a Service in Kubernetes?
- What does the `kubectl describe` command do?
- What does the `kubectl logs` command do?
- What is the difference between a Deployment and a StatefulSet?
- What is a DaemonSet?
- What is Ingress in Kubernetes?
- What is HPA (Horizontal Pod Autoscaler)?
- What are Readiness and Liveness Probes in Kubernetes?
